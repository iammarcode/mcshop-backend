spring.application.name=auth-service

# Profiles
spring.profiles.active=${SPRING_PROFILE:local}
# Server
server.port=8081
# MySQL
spring.datasource.username=local
spring.datasource.password=local
# SQL
spring.sql.init.mode=always
# Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Redis
spring.data.redis.port=6379

# SMTP
chatbot.mail.host=smtp.gmail.com
chatbot.mail.port=587
chatbot.mail.protocol=smtp
chatbot.mail.smtp-auth=true
chatbot.mail.smtp-starttls-enable=true
chatbot.mail.smtp-debug=false
chatbot.mail.username=${SMTP_USERNAME}
chatbot.mail.password=${SMTP_PASSWORD}
# OTP
chatbot.otp.expire.second=300

# Feign client
spring.cloud.openfeign.client.config.default.connectTimeout=5000
spring.cloud.openfeign.client.config.default.read-timeout=5000
spring.cloud.openfeign.client.config.default.logger-level=full

# Micro Service
user.service.name=user-service
user.service.path=/api/v1/user

# JWT
chatbot.jwt.secret-key=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
chatbot.jwt.access-token-expiration=86400000
chatbot.jwt.refresh-token-expiration=604800000